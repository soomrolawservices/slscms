// This service worker is now managed by vite-plugin-pwa.
// The generated service worker handles:
// - Precaching of all static assets (JS, CSS, HTML, images, fonts)
// - Runtime caching of Google Fonts, API calls, and storage files
// - Offline fallback support
// - Auto-update when new versions are deployed
//
// Push notification handling is now integrated into the PWA service worker
// via the workbox configuration in vite.config.ts
